spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/person_db?useTimezone=true&serverTimezone=UTC
    username: person
    password: person
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
